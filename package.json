{
  "name": "visa-consultancy-site",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint:html": "htmlhint '**/*.html'",
    "lint:css": "stylelint '**/*.css'",
    "lint:js": "eslint . --ext .js",
    "lint": "npm run lint:html && npm run lint:css && npm run lint:js"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "htmlhint": "^1.6.3",
    "stylelint": "^16.0.2",
    "stylelint-config-standard": "^36.0.0",
    "vite": "^5.0.0"
  }
}
